# Links
CONVERSATION_LINK_TEMPLATE="https://app.boomnow.com/dashboard/guest-experience/all?conversation={id}"
APP_ORIGIN="https://app.boomnow.com" # optional fallback
MESSAGES_URL=""                       # optional fallback
LOGIN_URL=""                          # optional fallback

# Data sources
CONVERSATIONS_URL="https://api.boomnow.com/conversations" # existing
PLATFORM_TOP50_URL="" # optional; if present, must return newest 50 already sorted

BOOM_API_BASE=https://api.boom.example
BOOM_API_TOKEN=your-token
BOOM_ORG_ID=your-org-id
# (Optional) CHECK_BATCH_SIZE=50  # if you still batch downstream processing
